import React from 'react'
import { Typography} from '@material-ui/core';

function Header() {
    return (
        <div align="center">
          <Typography variant="h2">Recipe App</Typography>
          <Typography>Explore different Categories of recipes and make the tastiest one today</Typography>
        </div>
    )
}

export default Header
